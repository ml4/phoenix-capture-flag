# Cache objects
packer_cache/

# Crash log
crash.log

# https://www.packer.io/guides/hcl/variables
# Exclude all .pkrvars.hcl files, which are likely to contain sensitive data,
# such as password, private keys, and other secrets. These should not be part of
# version control as they are data points which are potentially sensitive and
# subject to change depending on the environment.
#
*.pkrvars.hcl

# For built boxes
*.box

# files which contain public keys
keys

# terraform
.terraform.lock.hcl
terraform.tfstate*
.terraform

.terraform/providers/registry.terraform.io/hashicorp/aws/6.6.0/darwin_arm64/LICENSE.txt
.terraform/providers/registry.terraform.io/hashicorp/aws/6.6.0/darwin_arm64/terraform-provider-aws_v6.6.0_x5
